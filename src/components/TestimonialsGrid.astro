---
import { Icon } from 'astro-icon/components';
import { review } from '../content/data';
import MaleAvatar from '../assets/avatar.svg';
import FemaleAvatar from '../assets/female-avatar.svg';
---

<section id="testimonials" class="w-full py-16" data-animate>
  <h2 class="section-heading mb-12 text-center font-display text-2xl text-ink sm:text-3xl">
    Testimonials
  </h2>

  <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
    {
      review.map((item) => (
        <div class="paper-card relative flex flex-col rounded-sm border border-paper-edge bg-paper-deep/50 p-6">
          <Icon
            name="bi:quote"
            class="mb-4 h-8 w-8 text-mustard/60"
          />
          <p class="mb-6 flex-grow font-mono text-sm leading-relaxed text-ink-soft">
            "{item.body}"
          </p>
          <div class="flex items-center gap-3">
            <img
              src={item.img || (item.gender === 'F' ? FemaleAvatar.src : MaleAvatar.src)}
              alt={`${item.name} avatar`}
              class="h-10 w-10 rounded-full border border-paper-edge object-cover"
              loading="lazy"
            />
            <div>
              <p class="font-display text-ink">{item.name}</p>
              {(item.role || item.company) && (
                <p class="font-mono text-xs text-ink-soft">
                  {item.role}
                  {item.role && item.company && ', '}
                  {item.company && (
                    <a
                      href={item.ref}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-rust hover:underline"
                    >
                      {item.company}
                    </a>
                  )}
                </p>
              )}
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>
